"""
Módulo procesador.py

Define la clase base abstracta 'Procesador', utilizada como superclase
para los procesadores de texto, imagen y audio en el sistema EduMoodBot.
"""

from abc import ABC, abstractmethod
from typing import Any


class Procesador(ABC):
    """
    Clase base abstracta que define la estructura general de un procesador de datos.

    Atributos
    ----------
    datos : Any
        Datos genéricos que serán procesados por las subclases.

    Métodos Abstractos
    ------------------
    procesar() -> None
        Método abstracto que debe ser implementado por las subclases
        para realizar su respectivo procesamiento.
    """

    def __init__(self, datos: Any = None) -> None:
        self.datos = datos

    @abstractmethod
    def procesar(self) -> None:
        """Método abstracto que debe ser implementado por cada procesador específico."""
        pass